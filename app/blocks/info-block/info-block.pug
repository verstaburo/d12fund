mixin info-block(title, linktitle, link)
  - var addLink = "#" || link
  +b.info-block&attributes(attributes)
    +e.title!= title
    +e.content
      block
    +e.buttons
      +button('a')(href=addLink).__button!= linktitle
