extends ../../layouts/default

block head
  - var pageTitle = 'D12 Fund - Главная'

block content
  +page-section(false)._light.informers
    +grid
      +col(4)
        +info-block('Total Fund Value', 'View Statistics')
          +rate
          +income
      +col(4)
        +info-block('My Balance', 'Withdraw funds')
          +diagramm
          +income
      +col(4)
        +info-block('Purchases', 'View History')
          +accounting
  +page-section('Recent news').news
